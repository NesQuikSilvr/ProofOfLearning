#include <stdio.h>
#include <kipr/wombat.h>

/*
    Define two functions:
        - Take a servo position as input and move the servo ARM
        - Take a servo position as input and move the servo CLAW

    - The functions will only take one input each.
    - Save the servo ports as global variables.

    Rewrite the code you wrote in "basics.c" using your ARM and CLAW functions
*/

int main() {

    
    
    return 0;
}
